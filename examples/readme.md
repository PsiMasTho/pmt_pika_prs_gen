All generated files are already included, but you can regenerate them with `scripts/gen_example_*.py`
This requires `pmt_pika_prs_gen_cli` to be available in your `PATH` (build/install the CLI first).